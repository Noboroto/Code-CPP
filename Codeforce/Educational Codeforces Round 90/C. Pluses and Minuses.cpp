#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "C. Pluses and Minuses" + (string)".INP";
    const string FileOUT = "C. Pluses and Minuses" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    //freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    
    return 0;
}