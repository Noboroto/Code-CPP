#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "TSPtrace" + (string)".INP";
    const string FileOUT = "TSPtrace" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    //freopen (FileOUT.c_str(), "w", stdout);
}

int const N = 20 + 1;
int n, m;

void GetGraph ()
{
    
}

int main ()
{
    Init();
    cin >> n >> m;
    return 0;
}