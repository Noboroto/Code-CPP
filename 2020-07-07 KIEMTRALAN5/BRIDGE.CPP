#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "BRIDGE" + (string)".INP";
    const string FileOUT = "BRIDGE" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    //freopen (FileOUT.c_str(), "w", stdout);
}

void GetData ()
{

}

int main ()
{
    Init();
    return 0;
}