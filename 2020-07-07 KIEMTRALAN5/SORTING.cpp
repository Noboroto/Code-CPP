#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "SORTING" + (string)".INP";
    const string FileOUT = "SORTING" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << "NO";
    return 0;
}