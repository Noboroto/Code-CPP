#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "CACTUS" + (string)".INP";
    const string FileOUT = "CACTUS" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << -1;
    return 0;
}