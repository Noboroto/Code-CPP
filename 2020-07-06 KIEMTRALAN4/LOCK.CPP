#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "LOCK" + (string)".INP";
    const string FileOUT = "LOCK" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << 0;
    return 0;
}