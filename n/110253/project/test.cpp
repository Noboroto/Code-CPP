#include <bits/stdc++.h>

#define deb(x) std::cout << #x << x;
#define fo(i,n) for(i=0;i<n;i++)
#define Fo(i,k,n) for(i=k;i<n;i++)
#define downfo(i,n) for(i=n-1;i>=0;i--)

#define ll long long
#define vint std::vector<int>
#define vvint std::vector<std::vector<int>>

 int main()
 {
     //vint test(5,-1);
     //int i;
     //fo(i,test.size())
     //std::cout << test.at(i)<< "\n";

    std::stack<int> st;

 }
