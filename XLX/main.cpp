#include <cstdio>
#include <string>

using namespace std;

void start (){
    string x, y;
    scanf("%s ", &x);
    scanf("%s ", &y);
    printf("%s\n", x);
    printf("%s\n", y);
}

int main (){
    start();
    return 0;
}
