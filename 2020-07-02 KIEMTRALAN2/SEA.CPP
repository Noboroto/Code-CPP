#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "SEA" + (string)".INP";
    const string FileOUT = "SEA" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << -1;
    return 0;
}