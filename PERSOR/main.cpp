#include <iostream>
#include <cstdio>
#include <cmath>

using namespace std;

int main()
{
    freopen("PERSQR.INP", "r", stdin);
    freopen("PERSQR.OUT", "w", stdout);
    int t;
    cin >> t;
    for (int i = 1; i <= t; ++t){
        long long n;
        cin >> n;
        double ans = sqrt(n);

    }
    return 0;
}
