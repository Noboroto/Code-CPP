#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    freopen ("Tetris.INP", "r", stdin);
    freopen ("Tetris.OUT", "w", stdout);
}

int n, m;
char a[20][20];

struct Point 
{
    int x, y;
    Point(int _x = 0, int _y = 0)
    {
        x = _x;
        y = _y;
    }
};

bool Pic1 (Point Cent, char c)
{
    bool ans = false;
    if (Cent.x > 1 && Cent.y > 1)
    {
        ans = ()
    }
}

int main ()
{
    Init();
    cin >> n >> m;
    return 0;
}