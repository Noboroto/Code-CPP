#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "BRACKETS" + (string)".INP";
    const string FileOUT = "BRACKETS" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << 0;
    return 0;
}