#include <bits/stdc++.h>

using namespace std;

void Init ()
{
    const string FileINP = "BATTLE" + (string)".INP";
    const string FileOUT = "BATTLE" + (string)".OUT";
    freopen (FileINP.c_str(), "r", stdin);
    freopen (FileOUT.c_str(), "w", stdout);
}

int main ()
{
    Init();
    cout << "Strong is dark side of the force.";
    return 0;
}